"""itsme MCP surface — 3 verbs: remember / ask / status.

NOTE: this package is `itsme.mcp`, NOT to be confused with the `mcp` SDK
(modelcontextprotocol/python-sdk). Always import the SDK with its full
path (e.g. `from mcp.server.fastmcp import FastMCP`); src-layout keeps
them disambiguated.
"""
